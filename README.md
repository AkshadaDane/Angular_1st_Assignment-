# Angular Assignment 1

This project is part of my Angular assignments.  
It includes basic Angular components and functionality as a learning practice.

## How to Run
1. Clone the repository  
2. Run `npm install`  
3. Run `ng serve`  
4. Open `http://localhost:4200` in the browser
